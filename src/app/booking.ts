export interface BookingDetail {
    id: string
    name: string
    email: string
    phoneNumber: string
    reason: string
    room: string
    startDate: Date
    endDate: Date
}

export interface Detail {
    id: string
    name: string
    email: string
    phoneNumber: string
    reason: string
    room: string
    startDate: Date
    endDate: Date
    status: string
}
